extends layout.pug
include mixins/items_card

block content
    .d-flex.justify-content-center.mt-3
        h5 Items
    .album.py-5
      .container
        .row
          each item in items
            +thumbCard(item)

    script(src='js/delete.js')
